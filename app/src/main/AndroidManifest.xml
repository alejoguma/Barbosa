<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alejandroguma.barbosa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_barbosaicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".splashActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".drawertabActivity"
            android:label="@string/title_activity_drawertab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".drawerMainActivity"
            android:label="@string/title_activity_drawer_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".drawerPerfilActivity"
            android:label="@string/title_activity_drawer_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".iglesiactivity" />
        <activity android:name=".fiestaspActivity" />
        <activity android:name=".parqueActivity"></activity>
    </application>

</manifest>